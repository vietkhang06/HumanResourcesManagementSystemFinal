<Window x:Class="HumanResourcesManagementSystemFinal.Views.AddEmployeeWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:sys="clr-namespace:System;assembly=mscorlib" 
        Title="Thêm Nhân Viên" Height="680" Width="850"
        WindowStartupLocation="CenterScreen" WindowStyle="None" 
        AllowsTransparency="True" Background="Transparent" ResizeMode="NoResize"
        MouseLeftButtonDown="Window_MouseLeftButtonDown">

    <Window.Resources>
        <Style TargetType="TextBox">
            <Setter Property="Height" Value="40"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Padding" Value="10,0"/>
            <Setter Property="BorderBrush" Value="#E2E8F0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="1" CornerRadius="6">
                            <ScrollViewer x:Name="PART_ContentHost"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="TextBlock" x:Key="InputLabel">
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="#4A5568"/>
            <Setter Property="Margin" Value="0,10,0,5"/>
        </Style>
    </Window.Resources>

    <Border Background="White" CornerRadius="15" Margin="20">
        <Border.Effect>
            <DropShadowEffect BlurRadius="30" ShadowDepth="5" Opacity="0.15"/>
        </Border.Effect>

        <Grid Margin="40,28,40,28">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0">
                <TextBlock Text="Hồ Sơ Nhân Viên Mới" FontSize="24" FontWeight="Bold" Foreground="#1A3D64"/>
                <Separator Margin="0,15"/>
            </StackPanel>

            <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,20">
                <Border Width="70" Height="70" CornerRadius="35" BorderBrush="#E2E8F0" BorderThickness="1" Margin="0,0,15,0">
                    <Border.Background>
                        <ImageBrush x:Name="imgAvatarPreview" ImageSource="/Images/png2.png" Stretch="UniformToFill"/>
                    </Border.Background>
                </Border>
                <Button Content="Chọn ảnh..." Click="UploadImageButton_Click" VerticalAlignment="Center" Padding="10,5"/>
            </StackPanel>

            <Grid Grid.Row="2">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="30"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Margin="0,0,0,-5">
                    <TextBlock Text="Họ (Last Name)" Style="{StaticResource InputLabel}"/>
                    <TextBox x:Name="txtLastName"/>

                    <TextBlock Text="Tên (First Name)" Style="{StaticResource InputLabel}"/>
                    <TextBox x:Name="txtFirstName"/>

                    <TextBlock Text="Email" Style="{StaticResource InputLabel}"/>
                    <TextBox x:Name="txtEmail"/>

                    <TextBlock Text="Số điện thoại" Style="{StaticResource InputLabel}"/>
                    <TextBox x:Name="txtPhone"/>

                    <TextBlock Text="Địa chỉ (Address)" Style="{StaticResource InputLabel}"/>
                    <TextBox x:Name="txtAddress"/>
                </StackPanel>

                <StackPanel Grid.Column="2">
                    <TextBlock Text="Phòng ban" Style="{StaticResource InputLabel}"/>
                    <ComboBox x:Name="cboDepartment" Height="40" DisplayMemberPath="DepartmentName"/>

                    <TextBlock Text="Chức vụ" Style="{StaticResource InputLabel}"/>
                    <TextBox x:Name="txtPosition"/>

                    <TextBlock Text="Mức lương cơ bản (VNĐ)" Style="{StaticResource InputLabel}" Foreground="#E53E3E"/>
                    <TextBox x:Name="txtBaseSalary" PreviewTextInput="NumberValidationTextBox"/>

                    <TextBlock Text="Ngày bắt đầu" Style="{StaticResource InputLabel}" Foreground="#E53E3E"/>
                    <DatePicker x:Name="dpStartDate" Height="40" VerticalContentAlignment="Center" SelectedDate="{x:Static sys:DateTime.Now}"/>
                    <TextBlock Text="Người quản lý trực tiếp" Style="{StaticResource InputLabel}"/>

                    <ComboBox x:Name="cboManager" Height="40" Padding="10,0" VerticalContentAlignment="Center">
                        <ComboBox.ItemTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="{Binding EmployeeId}" FontWeight="Bold" Foreground="#1A3D64" Margin="0,0,5,0"/>
                                    <TextBlock Text="-" Margin="0,0,5,0"/>
                                    <TextBlock Text="{Binding FullName}" FontWeight="SemiBold"/>
                                    <TextBlock Text=" (" Foreground="Gray"/>
                                    <TextBlock Text="{Binding Position.Title}" Foreground="Gray" FontStyle="Italic"/>
                                    <TextBlock Text=")" Foreground="Gray"/>
                                </StackPanel>
                            </DataTemplate>
                        </ComboBox.ItemTemplate>
                    </ComboBox>
                </StackPanel>
            </Grid>

            <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
                <Button Content="Hủy" Click="CancelButton_Click" Width="100" Height="40" Margin="0,0,10,0"/>
                <Button Content="Lưu Hồ Sơ" Click="SaveButton_Click" Width="150" Height="40" Background="#1A3D64" Foreground="White" FontWeight="Bold"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>