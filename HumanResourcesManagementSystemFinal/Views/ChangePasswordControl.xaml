<UserControl x:Class="HumanResourcesManagementSystemFinal.Views.ChangePasswordControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             Background="Transparent">

    <UserControl.Resources>
        <Style TargetType="PasswordBox">
            <Setter Property="Height" Value="40"/>
            <Setter Property="Padding" Value="10,0"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Background" Value="#F8F9FA"/>
            <Setter Property="BorderBrush" Value="#E0E0E0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Margin" Value="0,5,0,20"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="PasswordBox">
                        <Border Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                CornerRadius="5">
                            <ScrollViewer x:Name="PART_ContentHost"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid>
        <Border Background="White" CornerRadius="15" Width="500" Height="450"
                VerticalAlignment="Center" HorizontalAlignment="Center">
            <Border.Effect>
                <DropShadowEffect BlurRadius="20" ShadowDepth="0" Opacity="0.1"/>
            </Border.Effect>

            <StackPanel Margin="40">
                <TextBlock Text="Đổi Mật Khẩu" FontSize="24" FontWeight="Bold" Foreground="#1A3D64" HorizontalAlignment="Center" Margin="0,0,0,30"/>

                <TextBlock Text="Mật khẩu hiện tại" Foreground="#7F8C8D"/>
                <PasswordBox x:Name="txtOldPass"/>

                <TextBlock Text="Mật khẩu mới" Foreground="#7F8C8D"/>
                <PasswordBox x:Name="txtNewPass"/>

                <TextBlock Text="Nhập lại mật khẩu mới" Foreground="#7F8C8D"/>
                <PasswordBox x:Name="txtConfirmPass"/>

                <Button Content="Xác Nhận Đổi" Height="45" Margin="0,10,0,0"
                        Click="BtnChange_Click" 
                        Background="#1A3D64" Foreground="White" FontSize="16" FontWeight="Bold" Cursor="Hand"
                        BorderThickness="0">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="5"/>
                        </Style>
                    </Button.Resources>
                </Button>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>