<UserControl x:Class="HumanResourcesManagementSystemFinal.Views.PayrollControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:vm="clr-namespace:HumanResourcesManagementSystemFinal.ViewModels"
             xmlns:cnv="clr-namespace:HumanResourcesManagementSystemFinal.Converters"
             mc:Ignorable="d" 
             d:DesignHeight="750" d:DesignWidth="1100" Background="Transparent">

    <UserControl.DataContext>
        <vm:PayrollViewModel/>
    </UserControl.DataContext>

    <UserControl.Resources>
        <Style x:Key="FilterTextBoxStyle" TargetType="TextBox">
            <Setter Property="Height" Value="30"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Padding" Value="5,0"/>
            <Setter Property="BorderBrush" Value="#E2E8F0"/>
            <Style.Resources>
                <Style TargetType="Border">
                    <Setter Property="CornerRadius" Value="4"/>
                </Style>
            </Style.Resources>
        </Style>
        <cnv:NameToInitialsConverter x:Key="NameToInitialsConverter"/>

        <cnv:BinaryImageConverter x:Key="BinaryImageConverter"/>

        <Geometry x:Key="IconMoney">M96 0C60.7 0 32 28.7 32 64s28.7 64 64 64l48 0 0 32-57 0c-31.6 0-58.5 23.1-63.3 54.4L1.1 364.1C.4 368.8 0 373.6 0 378.4L0 448c0 35.3 28.7 64 64 64l384 0c35.3 0 64-28.7 64-64l0-69.6c0-4.8-.4-9.6-1.1-14.4L488.2 214.4C483.5 183.1 456.6 160 425 160l-217 0 0-32 48 0c35.3 0 64-28.7 64-64S291.3 0 256 0L96 0z</Geometry>
        <Geometry x:Key="IconExport">M288 32c0-17.7-14.3-32-32-32s-32 14.3-32 32V274.7l-73.4-73.4c-12.5-12.5-32.8-12.5-45.3 0s-12.5 32.8 0 45.3l128 128c12.5 12.5 32.8 12.5 45.3 0l128-128c12.5-12.5 12.5-32.8 0-45.3s-32.8-12.5-45.3 0L288 274.7V32zM64 352c-35.3 0-64 28.7-64 64v32c0 35.3 28.7 64 64 64H448c35.3 0 64-28.7 64-64V416c0-35.3-28.7-64-64-64H346.5l-45.3 45.3c-25 25-65.5 25-90.5 0L165.5 352H64zM432 456a24 24 0 1 0 0-48 24 24 0 1 0 0 48z</Geometry>
        <Geometry x:Key="DownArrowGeometry">M7,10L12,15L17,10H7Z</Geometry>

        <Style x:Key="ModernComboBoxItemStyle" TargetType="{x:Type ComboBoxItem}">
            <Setter Property="SnapsToDevicePixels" Value="True"/>
            <Setter Property="Padding" Value="10"/>
            <Setter Property="HorizontalContentAlignment" Value="{Binding HorizontalContentAlignment, RelativeSource={RelativeSource AncestorType={x:Type ItemsControl}}}"/>
            <Setter Property="VerticalContentAlignment" Value="{Binding VerticalContentAlignment, RelativeSource={RelativeSource AncestorType={x:Type ItemsControl}}}"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ComboBoxItem}">
                        <Border x:Name="bd" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" Padding="{TemplateBinding Padding}" SnapsToDevicePixels="true">
                            <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" VerticalAlignment="{TemplateBinding VerticalContentAlignment}" SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsHighlighted" Value="True">
                                <Setter TargetName="bd" Property="Background" Value="#F1F5F9"/>
                                <Setter Property="Foreground" Value="#1A3D64"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ModernComboBoxStyle" TargetType="{x:Type ComboBox}">
            <Setter Property="ItemContainerStyle" Value="{StaticResource ModernComboBoxItemStyle}"/>
            <Setter Property="SnapsToDevicePixels" Value="true"/>
            <Setter Property="OverridesDefaultStyle" Value="true"/>
            <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Auto"/>
            <Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Auto"/>
            <Setter Property="ScrollViewer.CanContentScroll" Value="true"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="#334155"/>
            <Setter Property="Height" Value="35"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ComboBox}">
                        <Grid>
                            <ToggleButton Name="ToggleButton" 
                                              BorderBrush="{TemplateBinding BorderBrush}" 
                                              Background="{TemplateBinding Background}"
                                              Grid.Column="2" Focusable="false"
                                              IsChecked="{Binding Path=IsDropDownOpen,Mode=TwoWay,RelativeSource={RelativeSource TemplatedParent}}" ClickMode="Press">
                                <ToggleButton.Template>
                                    <ControlTemplate TargetType="{x:Type ToggleButton}">
                                        <Border x:Name="border" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="1" CornerRadius="6">
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition />
                                                    <ColumnDefinition Width="25" />
                                                </Grid.ColumnDefinitions>
                                                <Path x:Name="arrow" Grid.Column="1" HorizontalAlignment="Center" VerticalAlignment="Center" Data="{StaticResource DownArrowGeometry}" Fill="#64748B" Stretch="Uniform" Width="8"/>
                                            </Grid>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="true">
                                                <Setter TargetName="border" Property="BorderBrush" Value="#CBD5E1" />
                                                <Setter TargetName="border" Property="Background" Value="#F8FAFC" />
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </ToggleButton.Template>
                            </ToggleButton>
                            <ContentPresenter Name="ContentSite" IsHitTestVisible="False"  Content="{TemplateBinding SelectionBoxItem}" ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}" ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}" Margin="10,0,25,0" VerticalAlignment="Center" HorizontalAlignment="Center" />
                            <Popup Name="Popup" Placement="Bottom" IsOpen="{TemplateBinding IsDropDownOpen}" AllowsTransparency="True" Focusable="False" PopupAnimation="Slide">
                                <Grid Name="DropDown" SnapsToDevicePixels="True" MinWidth="{TemplateBinding ActualWidth}" MaxHeight="{TemplateBinding MaxDropDownHeight}">
                                    <Border x:Name="DropDownBorder" Background="White" BorderThickness="1" BorderBrush="#E2E8F0" CornerRadius="6" Margin="0,5,0,0">
                                        <Border.Effect>
                                            <DropShadowEffect BlurRadius="10" Color="Black" Opacity="0.1" ShadowDepth="2"/>
                                        </Border.Effect>
                                        <ScrollViewer Margin="0" SnapsToDevicePixels="True">
                                            <StackPanel IsItemsHost="True" KeyboardNavigation.DirectionalNavigation="Contained" />
                                        </ScrollViewer>
                                    </Border>
                                </Grid>
                            </Popup>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="HasItems" Value="false">
                                <Setter TargetName="DropDownBorder" Property="MinHeight" Value="95"/>
                            </Trigger>
                            <Trigger Property="IsGrouping" Value="true">
                                <Setter Property="ScrollViewer.CanContentScroll" Value="false"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="White"/>
            <Setter Property="Foreground" Value="#64748B"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Padding" Value="15,12"/>
            <Setter Property="BorderThickness" Value="0,0,0,2"/>
            <Setter Property="BorderBrush" Value="#F1F5F9"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
        </Style>

        <Style TargetType="DataGridRow">
            <Setter Property="Background" Value="White"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Height" Value="70"/>
            <Setter Property="BorderThickness" Value="0,0,0,1"/>
            <Setter Property="BorderBrush" Value="#F1F5F9"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#F8FAFC"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel VerticalAlignment="Center">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="Quản Lý Lương &amp; Thưởng" FontSize="24" FontWeight="Bold" Foreground="#1A3D64"/>
                </StackPanel>
                <TextBlock Text="Tính toán lương tự động dựa trên chấm công và hợp đồng" FontSize="13" Foreground="#64748B" Margin="0,5,0,0"/>
            </StackPanel>

            <Border Grid.Column="1" Background="White" CornerRadius="10" Padding="5" BorderThickness="1" BorderBrush="#E2E8F0">
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <ComboBox ItemsSource="{Binding Months}" SelectedItem="{Binding SelectedMonth}" 
                              Style="{StaticResource ModernComboBoxStyle}"
                              Width="75" Margin="0,0,5,0" BorderBrush="Transparent" Background="Transparent"/>

                    <TextBlock Text="/" VerticalAlignment="Center" Foreground="#CBD5E0" FontWeight="Bold" Margin="0,0,5,0"/>

                    <ComboBox ItemsSource="{Binding Years}" SelectedItem="{Binding SelectedYear}" 
                              Style="{StaticResource ModernComboBoxStyle}"
                              Width="85" BorderBrush="Transparent" Background="Transparent"/>

                    <Button Command="{Binding ExportToExcelCommand}" Margin="10,0,0,0"
                            Background="#10B981" Foreground="White" Height="35" Padding="15,0" BorderThickness="0" Cursor="Hand">
                        <Button.Resources>
                            <Style TargetType="Border">
                                <Setter Property="CornerRadius" Value="6"/>
                            </Style>
                        </Button.Resources>
                        <StackPanel Orientation="Horizontal">
                            <Path Data="{StaticResource IconExport}" Fill="White" Width="14" Height="14" Stretch="Uniform" Margin="0,0,8,0"/>
                            <TextBlock Text="XUẤT EXCEL" FontWeight="Bold" FontSize="12"/>
                        </StackPanel>
                    </Button>
                </StackPanel>
            </Border>
        </Grid>

        <Grid Grid.Row="1" Margin="0,0,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Background="White" CornerRadius="12" Margin="0,0,15,0" Padding="20">
                <Border.Effect>
                    <DropShadowEffect BlurRadius="15" Opacity="0.05" ShadowDepth="4" Color="Black"/>
                </Border.Effect>
                <StackPanel>
                    <TextBlock Text="Tổng Thực Lĩnh" Foreground="#64748B" FontSize="13" FontWeight="SemiBold"/>
                    <TextBlock Text="{Binding TotalSalaryFund, StringFormat={}{0:N0} VNĐ}" 
                               FontSize="24" FontWeight="Bold" Foreground="#2F855A" Margin="0,5,0,0"/>
                </StackPanel>
            </Border>

            <Border Grid.Column="1" Background="White" CornerRadius="12" Margin="0,0,15,0" Padding="20">
                <Border.Effect>
                    <DropShadowEffect BlurRadius="15" Opacity="0.05" ShadowDepth="4" Color="Black"/>
                </Border.Effect>
                <StackPanel>
                    <TextBlock Text="Nhân Sự Đã Tính" Foreground="#64748B" FontSize="13" FontWeight="SemiBold"/>
                    <TextBlock Text="{Binding PayrollList.Count, StringFormat={}{0} nhân viên}" 
                               FontSize="24" FontWeight="Bold" Foreground="#1A3D64" Margin="0,5,0,0"/>
                </StackPanel>
            </Border>

            <Border Grid.Column="2" Background="White" CornerRadius="12" Padding="20">
                <Border.Effect>
                    <DropShadowEffect BlurRadius="15" Opacity="0.05" ShadowDepth="4" Color="Black"/>
                </Border.Effect>
                <StackPanel>
                    <TextBlock Text="Trạng Thái" Foreground="#64748B" FontSize="13" FontWeight="SemiBold"/>
                    <TextBlock Text="{Binding PayrollStatus}" 
                               FontSize="24" FontWeight="Bold" 
                               Foreground="{Binding StatusColor}" 
                               Margin="0,5,0,0"/>
                </StackPanel>
            </Border>
        </Grid>

        <Border Grid.Row="2" Background="White" CornerRadius="12" Padding="0">
            <Border.Effect>
                <DropShadowEffect BlurRadius="15" Opacity="0.05" ShadowDepth="2" Color="Black"/>
            </Border.Effect>

            <DataGrid ItemsSource="{Binding PayrollList}" 
                      AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True"
                      GridLinesVisibility="Horizontal" HorizontalGridLinesBrush="#F1F5F9"
                      Background="Transparent" BorderThickness="0" HeadersVisibility="Column"
                      ScrollViewer.CanContentScroll="True"
                      RowHeight="70"
                      SelectionUnit="FullRow" SelectionMode="Single">

                <DataGrid.Resources>
                    <Style TargetType="DataGridCell">
                        <Setter Property="Padding" Value="15,0"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="DataGridCell">
                                    <Border Padding="{TemplateBinding Padding}" Background="Transparent">
                                        <ContentPresenter VerticalAlignment="Center"/>
                                    </Border>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                        <Style.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter Property="Background" Value="Transparent"/>
                                <Setter Property="BorderBrush" Value="Transparent"/>
                                <Setter Property="Foreground" Value="Black"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </DataGrid.Resources>

                <DataGrid.Columns>
                    <DataGridTemplateColumn Header="NHÂN VIÊN" Width="3*">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal" Margin="10,0">
                                    <Grid Width="45" Height="45" Margin="-10,0,15,0">
                                        <Ellipse Fill="#E0F2FE"/>
                                        <TextBlock Text="{Binding FullName, Converter={StaticResource NameToInitialsConverter}}" 
                                                   HorizontalAlignment="Center" VerticalAlignment="Center"
                                                   FontSize="16" FontWeight="Bold" Foreground="#0284C7"/>

                                        <Ellipse>
                                            <Ellipse.Fill>
                                                <ImageBrush ImageSource="{Binding AvatarData, Converter={StaticResource BinaryImageConverter}}" 
                                                            Stretch="UniformToFill"/>
                                            </Ellipse.Fill>
                                        </Ellipse>
                                        <Ellipse Stroke="#E2E8F0" StrokeThickness="1"/>
                                    </Grid>

                                    <StackPanel VerticalAlignment="Center">
                                        <TextBlock Text="{Binding FullName}" FontWeight="Bold" Foreground="#1E293B" FontSize="14"/>
                                        <StackPanel Orientation="Horizontal" Margin="0,4,0,0">
                                            <TextBlock Text="{Binding DepartmentName}" Foreground="#64748B" FontSize="12" VerticalAlignment="Center"/>
                                        </StackPanel>
                                    </StackPanel>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <DataGridTemplateColumn Header="CÔNG" Width="70">
                        <DataGridTemplateColumn.HeaderStyle>
                            <Style TargetType="DataGridColumnHeader" BasedOn="{StaticResource {x:Type DataGridColumnHeader}}">
                                <Setter Property="HorizontalContentAlignment" Value="Center"/>
                            </Style>
                        </DataGridTemplateColumn.HeaderStyle>
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Border Background="#F8FAFC" CornerRadius="8" Width="50" Height="32" HorizontalAlignment="Center">
                                    <TextBlock Text="{Binding ActualWorkDays}" FontWeight="Bold" Foreground="#334155" 
                                               HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <DataGridTextColumn Header="LƯƠNG HĐ" Binding="{Binding ContractSalary, StringFormat={}{0:N0}}" Width="140" 
                            FontWeight="SemiBold" Foreground="#64748B">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="VerticalAlignment" Value="Center"/>
                                <Setter Property="HorizontalAlignment" Value="Right"/>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                        <DataGridTextColumn.HeaderStyle>
                            <Style TargetType="DataGridColumnHeader" BasedOn="{StaticResource {x:Type DataGridColumnHeader}}">
                                <Setter Property="HorizontalContentAlignment" Value="Right"/>
                            </Style>
                        </DataGridTextColumn.HeaderStyle>
                    </DataGridTextColumn>

                    <DataGridTemplateColumn Header="TỔNG THU" Width="140">
                        <DataGridTemplateColumn.HeaderStyle>
                            <Style TargetType="DataGridColumnHeader" BasedOn="{StaticResource {x:Type DataGridColumnHeader}}">
                                <Setter Property="HorizontalContentAlignment" Value="Right" />
                            </Style>
                        </DataGridTemplateColumn.HeaderStyle>
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding TotalIncome, StringFormat={}{0:N0}}" 
                                           FontWeight="Bold" Foreground="#2563EB" FontSize="13"
                                           VerticalAlignment="Center" HorizontalAlignment="Right"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <DataGridTemplateColumn Header="TỔNG CHI" Width="140">
                        <DataGridTemplateColumn.HeaderStyle>
                            <Style TargetType="DataGridColumnHeader" BasedOn="{StaticResource {x:Type DataGridColumnHeader}}">
                                <Setter Property="HorizontalContentAlignment" Value="Right"/>
                            </Style>
                        </DataGridTemplateColumn.HeaderStyle>
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding TotalDeduction, StringFormat={}{0:N0}}" 
                                           FontWeight="Bold" Foreground="#EF4444" FontSize="13"
                                           VerticalAlignment="Center" HorizontalAlignment="Right"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <DataGridTemplateColumn Width="200">
                        <DataGridTemplateColumn.Header>
                            <Grid HorizontalAlignment="Stretch">
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="70,0,10,0">
                                    <TextBlock Text="THỰC LĨNH" Foreground="#64748B" FontSize="12" />
                                    <Button x:Name="BtnFilterSalary" 
                                            Background="Transparent" BorderThickness="0" Cursor="Hand" Margin="12,0,0,0"
                                            Command="{Binding DataContext.ToggleFilterCommand, RelativeSource={RelativeSource AncestorType=UserControl}}">
                                        <Path Data="M10 18h4v-2h-4v2zM3 6v2h18V6H3zm3 7h12v-2H6v2z" 
                                              Fill="#94A3B8" Stretch="Uniform" Width="14" Height="14"/>
                                        </Button>
                                    </StackPanel>

                                <Popup IsOpen="{Binding DataContext.IsFilterPopupOpen, RelativeSource={RelativeSource AncestorType=UserControl}, Mode=TwoWay}" StaysOpen="False" Placement="Bottom" PlacementTarget="{Binding ElementName=BtnFilterSalary}" AllowsTransparency="True" Focusable="True">
                                    <Border Background="White" CornerRadius="20" Padding="20" Margin="10" BorderThickness="1" BorderBrush="#F1F5F9">
                                        <Border.Effect>
                                            <DropShadowEffect BlurRadius="25" Color="#64748B"  Opacity="0.15" ShadowDepth="8"  Direction="270"/>
                                        </Border.Effect>
                                        <StackPanel Width="220" DataContext="{Binding DataContext, RelativeSource={RelativeSource AncestorType=UserControl}}">
                                            <TextBlock Text="Lọc Theo Mức Lương"  Margin="0,0,0,18"  FontSize="16"  Foreground="#1A3D64" FontWeight="Bold" HorizontalAlignment="Center"/>

                                            <TextBlock Text="Mức lương tối thiểu (VNĐ)" Margin="0,0,0,6" FontSize="12" Foreground="#64748B" FontWeight="SemiBold"/>

                                            <TextBox Text="{Binding MinSalary, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,15" Height="38" VerticalContentAlignment="Center" Padding="12,0" BorderBrush="#E2E8F0">
                                                <TextBox.Resources>
                                                    <Style TargetType="{x:Type Border}">
                                                        <Setter Property="CornerRadius" Value="10"/>
                                                    </Style>
                                                </TextBox.Resources>
                                            </TextBox>

                                            <TextBlock Text="Mức lương tối đa (VNĐ)" Margin="0,0,0,6" FontSize="12" Foreground="#64748B" FontWeight="SemiBold"/>

                                            <TextBox Text="{Binding MaxSalary, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,20" Height="38" VerticalContentAlignment="Center" Padding="12,0" BorderBrush="#E2E8F0">
                                                <TextBox.Resources>
                                                    <Style TargetType="{x:Type Border}">
                                                        <Setter Property="CornerRadius" Value="10"/>
                                                    </Style>
                                                </TextBox.Resources>
                                            </TextBox>

                                            <Button Content="Lọc ngay" Command="{Binding ApplyFilterCommand}" Height="42" Width="200" FontWeight="Bold" Background="#10B981"  Foreground="White" BorderThickness="0" Cursor="Hand">
                                                <Button.Resources>
                                                    <Style TargetType="Border">
                                                        <Setter Property="CornerRadius" Value="12"/>
                                                    </Style>
                                                </Button.Resources>
                                            </Button>

                                            <Button Content="Xóa lọc hiện tại" 
                                                    Command="{Binding ResetFilterCommand}" Background="Transparent" BorderThickness="0" Foreground="#94A3B8" FontSize="12" FontWeight="Medium" Cursor="Hand" Margin="0,10,0,0"/>
                                        </StackPanel>
                                    </Border>
                                </Popup>
                            </Grid>
                        </DataGridTemplateColumn.Header>

                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Border Background="#F0FDF4" CornerRadius="8" Padding="12,6" HorizontalAlignment="Right" Margin="0,0,10,0">
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="{Binding NetSalary, StringFormat={}{0:N0}}" 
                                                   FontWeight="Bold" Foreground="#15803D" FontSize="15"/>
                                        <TextBlock Text=" VNĐ" Foreground="#15803D" FontSize="12" VerticalAlignment="Bottom" Margin="2,0,0,2"/>
                                    </StackPanel>
                                </Border>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</UserControl>