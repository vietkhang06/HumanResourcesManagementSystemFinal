<UserControl x:Class="HumanResourcesManagementSystemFinal.Views.TimeSheetControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:vm="clr-namespace:HumanResourcesManagementSystemFinal.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="1000" Background="Transparent">

    <UserControl.DataContext>
        <vm:TimeSheetViewModel/>
    </UserControl.DataContext>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="350"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0" Background="#1A3D64" CornerRadius="10" Margin="0,0,20,0">
            <Border.Effect>
                <DropShadowEffect BlurRadius="15" Opacity="0.2" ShadowDepth="3"/>
            </Border.Effect>

            <Grid Margin="20">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0" Margin="0,30,0,0">
                    <TextBlock Text="{Binding CurrentTime}" Foreground="White" FontSize="48" FontWeight="Bold" HorizontalAlignment="Center"/>
                    <TextBlock Text="{Binding CurrentDate}" Foreground="#A0AEC0" FontSize="16" HorizontalAlignment="Center" Margin="0,-5,0,0"/>

                    <Border Background="#2C5282" CornerRadius="15" Padding="15,5" HorizontalAlignment="Center" Margin="0,20,0,0">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="● " Foreground="#48BB78"/>
                            <TextBlock Text="{Binding TodayStatus}" Foreground="White" FontWeight="SemiBold"/>
                        </StackPanel>
                    </Border>
                </StackPanel>

                <StackPanel Grid.Row="1" VerticalAlignment="Center">
                    <Button Command="{Binding CheckInCommand}" Height="60" Margin="0,0,0,20"
                            Background="#48BB78" BorderThickness="0" Cursor="Hand">
                        <Button.Resources>
                            <Style TargetType="Border">
                                <Setter Property="CornerRadius" Value="10"/>
                            </Style>
                        </Button.Resources>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="▶  " Foreground="White" FontSize="20"/>
                            <TextBlock Text="BẮT ĐẦU CA (Check In)" Foreground="White" FontSize="16" FontWeight="Bold" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Button>

                    <Button Command="{Binding CheckOutCommand}" Height="60"
                            Background="#F56565" BorderThickness="0" Cursor="Hand">
                        <Button.Resources>
                            <Style TargetType="Border">
                                <Setter Property="CornerRadius" Value="10"/>
                            </Style>
                        </Button.Resources>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="⏹  " Foreground="White" FontSize="20"/>
                            <TextBlock Text="KẾT THÚC CA (Check Out)" Foreground="White" FontSize="16" FontWeight="Bold" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Button>
                </StackPanel>

                <Border Grid.Row="2" Background="White" CornerRadius="10" Padding="15" Opacity="0.95">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0" HorizontalAlignment="Center">
                            <TextBlock Text="Giờ vào" Foreground="#718096" FontSize="12"/>
                            <TextBlock Text="{Binding CheckInTimeDisplay}" Foreground="#2D3748" FontWeight="Bold" FontSize="18"/>
                        </StackPanel>

                        <Rectangle Grid.Column="1" Width="1" Fill="#E2E8F0" Margin="10,0"/>

                        <StackPanel Grid.Column="2" HorizontalAlignment="Center">
                            <TextBlock Text="Giờ ra" Foreground="#718096" FontSize="12"/>
                            <TextBlock Text="{Binding CheckOutTimeDisplay}" Foreground="#2D3748" FontWeight="Bold" FontSize="18"/>
                        </StackPanel>
                    </Grid>
                </Border>
            </Grid>
        </Border>

        <Border Grid.Column="1" Background="White" CornerRadius="10">
            <Border.Effect>
                <DropShadowEffect BlurRadius="15" Opacity="0.1" ShadowDepth="2" Color="#ccc"/>
            </Border.Effect>
            <Grid Margin="20">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <Grid Grid.Row="0" Margin="0,0,0,20">
                    <StackPanel>
                        <TextBlock Text="📅 Lịch Sử Chấm Công" FontSize="20" FontWeight="Bold" Foreground="#1A3D64"/>
                        <TextBlock Text="Tháng 11/2025" Foreground="#718096" Margin="0,5,0,0"/>
                    </StackPanel>

                    <Button HorizontalAlignment="Right" Content="Xuất Báo Cáo" Background="#EDF2F7" Foreground="#2D3748" 
                            Padding="15,0" Height="35" BorderThickness="0" FontWeight="SemiBold">
                        <Button.Resources>
                            <Style TargetType="Border">
                                <Setter Property="CornerRadius" Value="5"/>
                            </Style>
                        </Button.Resources>
                    </Button>
                </Grid>

                <DataGrid Grid.Row="1" ItemsSource="{Binding TimeSheets}" AutoGenerateColumns="False" 
                          CanUserAddRows="False" IsReadOnly="True" GridLinesVisibility="Horizontal" 
                          Background="White" BorderThickness="0" HeadersVisibility="Column">

                    <DataGrid.Resources>
                        <Style TargetType="DataGridColumnHeader">
                            <Setter Property="Background" Value="Transparent"/>
                            <Setter Property="Foreground" Value="#718096"/>
                            <Setter Property="FontWeight" Value="SemiBold"/>
                            <Setter Property="Padding" Value="10"/>
                            <Setter Property="HorizontalContentAlignment" Value="Left"/>
                            <Setter Property="BorderThickness" Value="0,0,0,1"/>
                            <Setter Property="BorderBrush" Value="#E2E8F0"/>
                        </Style>
                        <Style TargetType="DataGridRow">
                            <Setter Property="Height" Value="50"/>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#F7FAFC"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                        <Style TargetType="DataGridCell">
                            <Setter Property="Padding" Value="10,0"/>
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="DataGridCell">
                                        <Border Padding="{TemplateBinding Padding}" Background="Transparent">
                                            <ContentPresenter VerticalAlignment="Center"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </DataGrid.Resources>

                    <DataGrid.Columns>
                        <DataGridTextColumn Header="NGÀY" Binding="{Binding Date, StringFormat=dd/MM/yyyy}" Width="1.5*" FontWeight="SemiBold" Foreground="#2D3748"/>

                        <DataGridTextColumn Header="CHECK IN" Binding="{Binding CheckInTime, StringFormat=hh\\:mm}" Width="*" Foreground="#48BB78"/>

                        <DataGridTextColumn Header="CHECK OUT" Binding="{Binding CheckOutTime, StringFormat=hh\\:mm}" Width="*" Foreground="#E53E3E"/>

                        <DataGridTemplateColumn Header="TỔNG GIỜ" Width="*">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding HoursWorked, StringFormat={}{0} giờ}" FontWeight="Bold" Foreground="#2B6CB0"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>

                        <DataGridTemplateColumn Header="TRẠNG THÁI" Width="120">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Border Background="#C6F6D5" CornerRadius="12" Padding="10,4" HorizontalAlignment="Left">
                                        <TextBlock Text="Đủ công" Foreground="#22543D" FontSize="11" FontWeight="Bold"/>
                                    </Border>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </Border>
    </Grid>
</UserControl>