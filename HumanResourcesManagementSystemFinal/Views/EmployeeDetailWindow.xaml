<Window x:Class="HumanResourcesManagementSystemFinal.Views.EmployeeDetailWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Thông Tin Chi Tiết Nhân Viên" Height="500" Width="700"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent">

    <Border Background="White" CornerRadius="15" BorderBrush="#E2E8F0" BorderThickness="1">
        <Border.Effect>
            <DropShadowEffect BlurRadius="20" Opacity="0.3" ShadowDepth="5"/>
        </Border.Effect>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Grid Grid.Row="0">
                <Border Height="100" CornerRadius="15,15,0,0" VerticalAlignment="Top">
                    <Border.Background>
                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                            <GradientStop Color="#1A3D64" Offset="0"/>
                            <GradientStop Color="#2C5282" Offset="1"/>
                        </LinearGradientBrush>
                    </Border.Background>
                </Border>

                <Button Content="✕" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="10"
                        Width="30" Height="30" Background="Transparent" Foreground="White" 
                        BorderThickness="0" Cursor="Hand" FontSize="16" Click="BtnClose_Click"/>

                <StackPanel VerticalAlignment="Bottom" Margin="30,50,0,-40" Orientation="Horizontal">
                    <Border Width="100" Height="100" CornerRadius="50" BorderThickness="4" BorderBrush="White" Background="White">
                        <Border.Effect>
                            <DropShadowEffect BlurRadius="10" Opacity="0.2"/>
                        </Border.Effect>
                        <Grid>
                            <Ellipse>
                                <Ellipse.Fill>
                                    <ImageBrush ImageSource="/Images/default_user.png" Stretch="UniformToFill"/>
                                </Ellipse.Fill>
                            </Ellipse>
                        </Grid>
                    </Border>

                    <StackPanel VerticalAlignment="Bottom" Margin="15,0,0,25">
                        <TextBlock Text="{Binding FullName}" Foreground="White" FontSize="22" FontWeight="Bold">
                            <TextBlock.Effect>
                                <DropShadowEffect BlurRadius="4" ShadowDepth="2" Opacity="0.5"/>
                            </TextBlock.Effect>
                        </TextBlock>
                        <TextBlock Text="{Binding Position.PositionName}" Foreground="#E2E8F0" FontSize="14"/>
                    </StackPanel>
                </StackPanel>
            </Grid>

            <Grid Grid.Row="1" Margin="30,60,30,20">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="30"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0">
                    <TextBlock Text="THÔNG TIN CÁ NHÂN" Foreground="#1A3D64" FontWeight="Bold" Margin="0,0,0,15"/>

                    <Grid Margin="0,0,0,10">
                        <TextBlock Text="Mã nhân viên:" Foreground="#718096"/>
                        <TextBlock Text="{Binding EmployeeID}" FontWeight="SemiBold" HorizontalAlignment="Right" Foreground="#2D3748"/>
                    </Grid>
                    <Grid Margin="0,0,0,10">
                        <TextBlock Text="Ngày sinh:" Foreground="#718096"/>
                        <TextBlock Text="{Binding DateOfBirth, StringFormat='dd/MM/yyyy'}" FontWeight="SemiBold" HorizontalAlignment="Right" Foreground="#2D3748"/>
                    </Grid>
                    <Grid Margin="0,0,0,10">
                        <TextBlock Text="Giới tính:" Foreground="#718096"/>
                        <TextBlock Text="{Binding Gender}" FontWeight="SemiBold" HorizontalAlignment="Right" Foreground="#2D3748"/>
                    </Grid>
                    <Grid Margin="0,0,0,10">
                        <TextBlock Text="CCCD/CMND:" Foreground="#718096"/>
                        <TextBlock Text="{Binding CCCD}" FontWeight="SemiBold" HorizontalAlignment="Right" Foreground="#2D3748"/>
                    </Grid>

                    <Separator Margin="0,10"/>

                    <TextBlock Text="LIÊN HỆ" Foreground="#1A3D64" FontWeight="Bold" Margin="0,0,0,15"/>
                    <Grid Margin="0,0,0,10">
                        <TextBlock Text="Email:" Foreground="#718096"/>
                        <TextBlock Text="{Binding Email}" FontWeight="SemiBold" HorizontalAlignment="Right" Foreground="#2D3748"/>
                    </Grid>
                    <Grid Margin="0,0,0,10">
                        <TextBlock Text="Điện thoại:" Foreground="#718096"/>
                        <TextBlock Text="{Binding PhoneNumber}" FontWeight="SemiBold" HorizontalAlignment="Right" Foreground="#2D3748"/>
                    </Grid>
                </StackPanel>

                <StackPanel Grid.Column="2">
                    <TextBlock Text="CÔNG VIỆC" Foreground="#1A3D64" FontWeight="Bold" Margin="0,0,0,15"/>

                    <Grid Margin="0,0,0,10">
                        <TextBlock Text="Phòng ban:" Foreground="#718096"/>
                        <TextBlock Text="{Binding Department.DepartmentName}" FontWeight="SemiBold" HorizontalAlignment="Right" Foreground="#2D3748"/>
                    </Grid>
                    <Grid Margin="0,0,0,10">
                        <TextBlock Text="Quản lý trực tiếp:" Foreground="#718096"/>
                        <TextBlock Text="{Binding ManagerID}" FontWeight="SemiBold" HorizontalAlignment="Right" Foreground="#2D3748"/>
                    </Grid>
                    <Grid Margin="0,0,0,10">
                        <TextBlock Text="Trạng thái:" Foreground="#718096"/>
                        <Border Background="#C6F6D5" CornerRadius="4" HorizontalAlignment="Right" Padding="8,2">
                            <TextBlock Text="{Binding Status}" Foreground="#22543D" FontWeight="Bold" FontSize="11"/>
                        </Border>
                    </Grid>

                    <Separator Margin="0,10"/>

                    <TextBlock Text="ĐỊA CHỈ LIÊN HỆ" Foreground="#1A3D64" FontWeight="Bold" Margin="0,0,0,10"/>
                    <TextBlock Text="{Binding Address}" TextWrapping="Wrap" Foreground="#4A5568" LineHeight="20"/>
                </StackPanel>
            </Grid>

            <Border Grid.Row="2" Background="#F7FAFC" CornerRadius="0,0,15,15" Padding="20">
                <Button Content="Đóng" HorizontalAlignment="Right" Width="100" Height="35"
                        Background="White" BorderBrush="#E2E8F0" BorderThickness="1"
                        Foreground="#2D3748" Click="BtnClose_Click">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="6"/>
                        </Style>
                    </Button.Resources>
                </Button>
            </Border>
        </Grid>
    </Border>
</Window>