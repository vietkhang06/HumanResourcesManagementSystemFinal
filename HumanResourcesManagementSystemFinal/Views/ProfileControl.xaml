<UserControl x:Class="HumanResourcesManagementSystemFinal.Views.ProfileControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:vm="clr-namespace:HumanResourcesManagementSystemFinal.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="1000" Background="Transparent">

    <UserControl.DataContext>
        <vm:ProfileViewModel/>
    </UserControl.DataContext>

    <UserControl.Resources>
        <Style TargetType="TextBox">
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Padding" Value="5,0"/>
            <Setter Property="Height" Value="35"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0,0,0,1"/>
            <Setter Property="BorderBrush" Value="#E2E8F0"/>
            <Style.Triggers>
                <Trigger Property="IsReadOnly" Value="True">
                    <Setter Property="BorderThickness" Value="0"/>
                    <Setter Property="Foreground" Value="#2D3748"/>
                </Trigger>
                <Trigger Property="IsReadOnly" Value="False">
                    <Setter Property="Background" Value="#F7FAFC"/>
                    <Setter Property="BorderBrush" Value="#3182CE"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="350"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0" Background="White" CornerRadius="10" Margin="0,0,20,0">
            <Border.Effect>
                <DropShadowEffect BlurRadius="15" Opacity="0.1" ShadowDepth="2" Color="#ccc"/>
            </Border.Effect>
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <Border Grid.Row="0" Height="120" CornerRadius="10,10,0,0" Background="#1A3D64"/>

                <Grid Grid.Row="0" Grid.RowSpan="2" VerticalAlignment="Bottom" Margin="0,0,0,0">
                    <Border Width="130" Height="130" CornerRadius="65" Background="White" BorderThickness="4" BorderBrush="White">
                        <Border.Effect>
                            <DropShadowEffect BlurRadius="10" Opacity="0.2"/>
                        </Border.Effect>
                        <Grid>
                            <Image Source="/Images/png1.png" Stretch="UniformToFill" HorizontalAlignment="Center" VerticalAlignment="Center"/>

                            <Border Background="Black" CornerRadius="65" Opacity="0"/>
                            <Grid.OpacityMask>
                                <VisualBrush>
                                    <VisualBrush.Visual>
                                        <Border Background="Black" Width="120" Height="120" CornerRadius="60"/>
                                    </VisualBrush.Visual>
                                </VisualBrush>
                            </Grid.OpacityMask>
                        </Grid>
                    </Border>
                </Grid>

                <StackPanel Grid.Row="1" Margin="0,70,0,30" HorizontalAlignment="Center">
                    <TextBlock Text="{Binding CurrentUser.FullName}" FontSize="22" FontWeight="Bold" Foreground="#2D3748" HorizontalAlignment="Center"/>
                    <TextBlock Text="{Binding CurrentUser.Position.Title}" FontSize="14" Foreground="#718096" HorizontalAlignment="Center" Margin="0,5,0,0"/>

                    <Border Background="#EBF8FF" CornerRadius="15" Padding="15,5" HorizontalAlignment="Center" Margin="0,15,0,0">
                        <TextBlock Text="{Binding AccountRole}" Foreground="#2B6CB0" FontWeight="Bold" FontSize="12"/>
                    </Border>
                </StackPanel>

                <StackPanel Grid.Row="2" Margin="30,0">
                    <TextBlock Text="THÔNG TIN LIÊN HỆ" FontSize="12" FontWeight="Bold" Foreground="#A0AEC0" Margin="0,0,0,15"/>

                    <StackPanel Orientation="Horizontal" Margin="0,0,0,15">
                        <TextBlock Text="📧" Width="30" FontSize="16"/>
                        <TextBlock Text="{Binding CurrentUser.Email}" VerticalAlignment="Center" Foreground="#4A5568"/>
                    </StackPanel>

                    <StackPanel Orientation="Horizontal" Margin="0,0,0,15">
                        <TextBlock Text="📱" Width="30" FontSize="16"/>
                        <TextBlock Text="{Binding CurrentUser.PhoneNumber}" VerticalAlignment="Center" Foreground="#4A5568"/>
                    </StackPanel>

                    <StackPanel Orientation="Horizontal" Margin="0,0,0,15">
                        <TextBlock Text="🏢" Width="30" FontSize="16"/>
                        <TextBlock Text="{Binding CurrentUser.Department.DepartmentName}" VerticalAlignment="Center" Foreground="#4A5568"/>
                    </StackPanel>
                </StackPanel>
            </Grid>
        </Border>

        <Border Grid.Column="1" Background="White" CornerRadius="10">
            <Border.Effect>
                <DropShadowEffect BlurRadius="15" Opacity="0.1" ShadowDepth="2" Color="#ccc"/>
            </Border.Effect>
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <StackPanel Margin="40">

                    <Grid Margin="0,0,0,30">
                        <StackPanel>
                            <TextBlock Text="Hồ Sơ Chi Tiết" FontSize="20" FontWeight="Bold" Foreground="#1A3D64"/>
                            <TextBlock Text="Quản lý thông tin cá nhân và bảo mật" FontSize="12" Foreground="#718096" Margin="0,5,0,0"/>
                        </StackPanel>

                        <Button Command="{Binding ToggleEditCommand}" 
                                HorizontalAlignment="Right" VerticalAlignment="Top"
                                Background="#3182CE" Foreground="White" BorderThickness="0" 
                                Padding="20,8" Cursor="Hand">
                            <Button.Resources>
                                <Style TargetType="Border">
                                    <Setter Property="CornerRadius" Value="6"/>
                                </Style>
                            </Button.Resources>
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="{Binding IsEditing, Converter={StaticResource BooleanToTextConverter}, FallbackValue='✏️ Chỉnh Sửa'}"/>
                            </StackPanel>
                        </Button>
                    </Grid>

                    <TextBlock Text="👤 Thông Tin Cá Nhân" FontSize="16" FontWeight="Bold" Foreground="#2D3748" Margin="0,0,0,20"/>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="20"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <StackPanel Grid.Column="0" Grid.Row="0" Margin="0,0,0,20">
                            <TextBlock Text="Họ và Tên" Foreground="#718096" Margin="0,0,0,5"/>
                            <TextBox Text="{Binding CurrentUser.FullName}" IsReadOnly="{Binding IsEditing, Converter={StaticResource InverseBooleanConverter}, FallbackValue=True}"/>
                        </StackPanel>
                        <StackPanel Grid.Column="2" Grid.Row="0" Margin="0,0,0,20">
                            <TextBlock Text="Ngày Sinh" Foreground="#718096" Margin="0,0,0,5"/>
                            <DatePicker SelectedDate="{Binding CurrentUser.DateOfBirth}" IsEnabled="{Binding IsEditing}" BorderThickness="0" VerticalAlignment="Center"/>
                        </StackPanel>

                        <StackPanel Grid.Column="0" Grid.Row="1" Margin="0,0,0,20">
                            <TextBlock Text="Giới Tính" Foreground="#718096" Margin="0,0,0,5"/>
                            <TextBox Text="{Binding CurrentUser.Gender}" IsReadOnly="{Binding IsEditing, Converter={StaticResource InverseBooleanConverter}, FallbackValue=True}"/>
                        </StackPanel>
                        <StackPanel Grid.Column="2" Grid.Row="1" Margin="0,0,0,20">
                            <TextBlock Text="Địa Chỉ" Foreground="#718096" Margin="0,0,0,5"/>
                            <TextBox Text="{Binding CurrentUser.Address}" IsReadOnly="{Binding IsEditing, Converter={StaticResource InverseBooleanConverter}, FallbackValue=True}"/>
                        </StackPanel>
                    </Grid>

                    <Separator Background="#EDF2F7" Margin="0,10,0,30"/>

                    <TextBlock Text="💼 Thông Tin Công Việc" FontSize="16" FontWeight="Bold" Foreground="#2D3748" Margin="0,0,0,20"/>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="20"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <StackPanel Grid.Column="0" Grid.Row="0" Margin="0,0,0,20">
                            <TextBlock Text="Mã Nhân Viên" Foreground="#718096" Margin="0,0,0,5"/>
                            <TextBox Text="{Binding CurrentUser.Id}" IsReadOnly="True" Background="#F7FAFC" Foreground="#A0AEC0"/>
                        </StackPanel>
                        <StackPanel Grid.Column="2" Grid.Row="0" Margin="0,0,0,20">
                            <TextBlock Text="Ngày Vào Làm" Foreground="#718096" Margin="0,0,0,5"/>
                            <DatePicker SelectedDate="{Binding CurrentUser.HireDate}" IsEnabled="False" BorderThickness="0"/>
                        </StackPanel>

                        <StackPanel Grid.Column="0" Grid.Row="1" Margin="0,0,0,20">
                            <TextBlock Text="Phòng Ban" Foreground="#718096" Margin="0,0,0,5"/>
                            <TextBox Text="{Binding CurrentUser.Department.DepartmentName}" IsReadOnly="True" Background="#F7FAFC"/>
                        </StackPanel>
                        <StackPanel Grid.Column="2" Grid.Row="1" Margin="0,0,0,20">
                            <TextBlock Text="Chức Vụ" Foreground="#718096" Margin="0,0,0,5"/>
                            <TextBox Text="{Binding CurrentUser.Position.Title}" IsReadOnly="True" Background="#F7FAFC"/>
                        </StackPanel>
                    </Grid>

                    <Separator Background="#EDF2F7" Margin="0,10,0,30"/>

                    <TextBlock Text="🔒 Bảo Mật Tài Khoản" FontSize="16" FontWeight="Bold" Foreground="#2D3748" Margin="0,0,0,20"/>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="20"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0">
                            <TextBlock Text="Tên Đăng Nhập" Foreground="#718096" Margin="0,0,0,5"/>
                            <TextBox Text="{Binding Username}" IsReadOnly="True" FontWeight="Bold"/>
                        </StackPanel>

                        <Button Grid.Column="2" Command="{Binding ChangePasswordCommand}"
                                Content="Đổi Mật Khẩu" HorizontalAlignment="Left" VerticalAlignment="Bottom"
                                Background="#EDF2F7" Foreground="#2D3748" FontWeight="SemiBold"
                                Padding="20,8" BorderThickness="0" Cursor="Hand">
                            <Button.Resources>
                                <Style TargetType="Border">
                                    <Setter Property="CornerRadius" Value="5"/>
                                </Style>
                            </Button.Resources>
                        </Button>
                    </Grid>

                </StackPanel>
            </ScrollViewer>
        </Border>
    </Grid>
</UserControl>