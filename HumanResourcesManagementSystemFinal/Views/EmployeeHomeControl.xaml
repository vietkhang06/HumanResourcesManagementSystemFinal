<UserControl x:Class="HumanResourcesManagementSystemFinal.Views.EmployeeHomeControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:vm="clr-namespace:HumanResourcesManagementSystemFinal.ViewModels"
             xmlns:converters="clr-namespace:HumanResourcesManagementSystemFinal.Converters"
             mc:Ignorable="d" 
             d:DesignHeight="850" d:DesignWidth="1200" Background="Transparent">

    <UserControl.DataContext>
        <vm:EmployeeHomeViewModel/>
    </UserControl.DataContext>

    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>
        <converters:BinaryImageConverter x:Key="BinaryImageConverter"/>

        <Style x:Key="HeaderStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="#1E293B"/>
            <Setter Property="Margin" Value="0,0,0,15"/>
        </Style>

        <Style x:Key="CardPanel" TargetType="Border">
            <Setter Property="Background" Value="White"/>
            <Setter Property="CornerRadius" Value="12"/>
            <Setter Property="Padding" Value="20"/>
            <Setter Property="Margin" Value="0,0,0,20"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect BlurRadius="15" Opacity="0.05" ShadowDepth="4" Color="Black"/>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="0,0,10,0">
        <Grid Margin="15">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0" Margin="0,10,0,25">
                <TextBlock Text="Chào mừng quay trở lại," FontSize="16" Foreground="#64748B"/>
                <TextBlock Text="{Binding WelcomeMessage, FallbackValue='Nhân Viên'}" 
                           FontSize="28" FontWeight="ExtraBold" Foreground="#1E3A8A"/>
            </StackPanel>

            <UniformGrid Grid.Row="1" Columns="4" Margin="0,0,244,15">
                <Border Style="{StaticResource CardPanel}" Margin="0,0,5,0">
                    <StackPanel VerticalAlignment="Center">
                        <TextBlock Text="Vào Ca Lúc" FontSize="12" Foreground="#64748B" FontWeight="SemiBold" Margin="0,0,0,5"/>
                        <TextBlock Text="{Binding TodayCheckInTime, FallbackValue='--:--'}" FontSize="28" FontWeight="Bold" Foreground="#2C7A7B"/>
                    </StackPanel>
                </Border>
                <Border Style="{StaticResource CardPanel}" Margin="0,0,5,0">
                    <StackPanel VerticalAlignment="Center">
                        <TextBlock Text="Phép Còn Lại" FontSize="12" Foreground="#64748B" FontWeight="SemiBold" Margin="0,0,0,5"/>
                        <TextBlock Text="{Binding RemainingLeaveDays, FallbackValue=12}" FontSize="28" FontWeight="Bold" Foreground="#D69E2E"/>
                    </StackPanel>
                </Border>
                <Border Style="{StaticResource CardPanel}" Margin="0,0,-25,0">
                    <StackPanel VerticalAlignment="Center">
                        <TextBlock Text="Công Tháng Này" FontSize="12" Foreground="#64748B" FontWeight="SemiBold" Margin="0,0,0,5"/>
                        <TextBlock Text="{Binding DaysWorkedThisMonth, FallbackValue=0 ngày, StringFormat=\{0\} ngày}" FontSize="28" FontWeight="Bold" Foreground="#2B6CB0"/>
                    </StackPanel>
                </Border>
            </UniformGrid>

            <Grid Grid.Row="2">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="684*"/>
                    <ColumnDefinition Width="20"/>
                    <ColumnDefinition Width="85*"/>
                    <ColumnDefinition Width="371*"/>
                </Grid.ColumnDefinitions>

                <Border Grid.Column="0" Style="{StaticResource CardPanel}" Grid.ColumnSpan="3" Margin="0,0,70,20">
                    <StackPanel>
                        <TextBlock Text="📋 Trạng Thái Đơn Của Tôi" Style="{StaticResource HeaderStyle}"/>
                        <DataGrid ItemsSource="{Binding MyLeaveRequests}" AutoGenerateColumns="False" 
                                  CanUserAddRows="False" IsReadOnly="True" BorderThickness="0" Background="White">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="LÝ DO" Binding="{Binding Reason}" Width="2*" FontWeight="SemiBold"/>
                                <DataGridTextColumn Header="THỜI GIAN" Binding="{Binding StartDate, StringFormat=dd/MM}" Width="*"/>
                                <DataGridTemplateColumn Header="TRẠNG THÁI" Width="Auto">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <Border CornerRadius="10" Padding="8,2" Background="#FEFCBF" HorizontalAlignment="Left">
                                                <TextBlock Text="{Binding Status}" FontSize="11" FontWeight="Bold" Foreground="#744210"/>
                                            </Border>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>
                            </DataGrid.Columns>
                        </DataGrid>
                    </StackPanel>
                </Border>

                <StackPanel Grid.Column="2" Margin="20,-112,20,0" Grid.ColumnSpan="2">

                    <Border Style="{StaticResource CardPanel}" Height="256" Width="410">
                        <StackPanel>
                            <TextBlock Text="Thông Báo Mới" Style="{StaticResource HeaderStyle}"/>
                            <TextBlock Text="Không có thông báo mới" FontSize="12" Foreground="#94A3B8" FontStyle="Italic" HorizontalAlignment="Center" Margin="0,50,0,0"/>
                        </StackPanel>
                    </Border>
                    <Border Style="{StaticResource CardPanel}" Height="171" Width="414">
                        <Border Style="{StaticResource CardPanel}" Margin="-20,0,-20,-10">
                            <StackPanel Margin="-20,-33,-20,-28">
                                <Border Style="{StaticResource CardPanel}" Width="379" Height="169">
                                    <StackPanel>

                                        <TextBlock Text="Lối Tắt" Style="{StaticResource HeaderStyle}" Width="346"/>

                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="10"/>
                                                <ColumnDefinition Width="*"/>
                                            </Grid.ColumnDefinitions>

                                            <Button Grid.Column="0" Command="{Binding OpenLeavePopupCommand}" Cursor="Hand" BorderThickness="0" Margin="-17,0,22,10">
                                                <Button.Template>
                                                    <ControlTemplate TargetType="{x:Type Button}">
                                                        <Border Background="White" BorderBrush="#E2E8F0" BorderThickness="1" CornerRadius="12" Padding="10">
                                                            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                                                                <TextBlock Text="📝" FontSize="20" HorizontalAlignment="Center" Margin="0,0,0,5"/>
                                                                <TextBlock Text="Tạo Đơn Xin Nghỉ" FontWeight="Bold" Foreground="#2D3748" FontSize="13" HorizontalAlignment="Center"/>
                                                            </StackPanel>
                                                        </Border>
                                                    </ControlTemplate>
                                                </Button.Template>
                                            </Button>

                                            <Button Grid.Column="2" Command="{Binding QuickCheckInCommand}" Cursor="Hand" BorderThickness="0" Margin="10,0,0,10">
                                                <Button.Template>
                                                    <ControlTemplate TargetType="{x:Type Button}">
                                                        <Border Background="#1A3D64" CornerRadius="12" Padding="10">
                                                            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                                                                <TextBlock Text="🕒" FontSize="20" HorizontalAlignment="Center" Margin="0,0,0,5" Foreground="White"/>
                                                                <TextBlock Text="Vào Ca làm Việc" FontWeight="Bold" Foreground="White" FontSize="13" HorizontalAlignment="Center"/>
                                                            </StackPanel>
                                                        </Border>
                                                    </ControlTemplate>
                                                </Button.Template>
                                            </Button>
                                        </Grid>
                                    </StackPanel>
                                </Border>
                            </StackPanel>
                        </Border>
                    </Border>
                </StackPanel>
            </Grid>
        </Grid>
    </ScrollViewer>
</UserControl>