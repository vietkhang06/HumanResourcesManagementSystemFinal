<UserControl x:Class="HumanResourcesManagementSystemFinal.Views.ForgotPasswordControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" d:DesignWidth="400" Height="480">

    <UserControl.Resources>
        <Style TargetType="{x:Type TextBox}">
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type TextBox}">
                        <Border x:Name="border" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                Background="{TemplateBinding Background}"
                                CornerRadius="8">
                            <ScrollViewer x:Name="PART_ContentHost" Focusable="False" 
                                          HorizontalScrollBarVisibility="Hidden" 
                                          VerticalScrollBarVisibility="Hidden"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="#102f76"/>
                            </Trigger>
                            <Trigger Property="IsKeyboardFocused" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="#102f76"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid Margin="40">
        <StackPanel VerticalAlignment="Center" Height="386">

            <TextBlock Text="Quên Mật Khẩu?" 
                       FontSize="28" 
                       FontWeight="Bold" 
                       Foreground="#102f76" 
                       Margin="0,0,0,5" 
                       Height="40"/>

            <TextBlock Text="Tên đăng nhập" FontWeight="SemiBold" Foreground="#102f76"/>
            <TextBox x:Name="UsernameTextBox" 
                     Height="30" 
                     Padding="10, 0, 0, 0"
                     Background="White" 
                     Foreground="Black" 
                     BorderBrush="#CCCCCC" 
                     BorderThickness="1"
                     Margin="0,0,0,5" Width="315"/>

            <TextBlock Text="Số điện thoại" FontWeight="SemiBold" Foreground="#102f76" Margin="0,5,0,2"/>
            <TextBox x:Name="PhoneTextBox" 
                     Height="35" 
                     Padding="10, 0, 0, 0"
                     Background="White" 
                     Foreground="Black" 
                     BorderBrush="#CCCCCC" 
                     BorderThickness="1"
                     Margin="0,0,0,5" Width="315"/>

            <TextBlock Text="Email đăng ký" FontWeight="SemiBold" Foreground="#102f76" Margin="0,5,0,2"/>
            <TextBox x:Name="EmailInputTextBox" 
                     Height="35" 
                     Padding="10, 0, 0, 0"
                     Background="White" 
                     Foreground="Black" 
                     BorderBrush="#CCCCCC" 
                     BorderThickness="1"
                     Margin="0,0,0,5" Width="316"/>

            <TextBlock Text="CCCD" FontWeight="SemiBold" Foreground="#102f76" Margin="0,5,0,2"/>
            <TextBox x:Name="CCCDTextBox" 
                     Height="35" 
                     Padding="10, 0, 0, 0"
                     Background="White" 
                     Foreground="Black" 
                     BorderBrush="#CCCCCC" 
                     BorderThickness="1"
                     Margin="0,0,0,15" Width="315"/>

            <Button Content="GỬI YÊU CẦU"
                    Height="35"
                    Margin="0,20,0,0"
                    Background="#102f76" Foreground="#f9f8f6"
                    FontSize="16" FontWeight="Bold" Cursor="Hand" BorderThickness="0">
                <Button.Resources>
                    <Style TargetType="Border">
                        <Setter Property="CornerRadius" Value="8"/>
                    </Style>
                </Button.Resources>
            </Button>

            <Button Command="{Binding SwitchToLoginCommand}"
                    Content="← Quay lại Đăng nhập" 
                    HorizontalAlignment="Center" Margin="0,10,0,0"
                    Background="Transparent" BorderThickness="0" 
                    Foreground="#102f76" Cursor="Hand" FontSize="14"/>
        </StackPanel>
    </Grid>
</UserControl>