<UserControl x:Class="HumanResourcesManagementSystemFinal.Views.PayrollControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:vm="clr-namespace:HumanResourcesManagementSystemFinal.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="1000" Background="Transparent">

    <UserControl.DataContext>
        <vm:PayrollViewModel/>
    </UserControl.DataContext>

    <UserControl.Resources>
        <Style TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#718096"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="10,0,10,10"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="BorderThickness" Value="0,0,0,1"/>
            <Setter Property="BorderBrush" Value="#E2E8F0"/>
        </Style>
        <Style TargetType="DataGridRow">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderThickness" Value="0,0,0,1"/>
            <Setter Property="BorderBrush" Value="#F7FAFC"/>
            <Setter Property="Height" Value="50"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#F0FFF4"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel>
                <TextBlock Text="💰 Bảng Tính Lương" FontSize="24" FontWeight="Bold" Foreground="#1A3D64"/>
                <TextBlock Text="Dữ liệu được tính dựa trên Hợp đồng &amp; Chấm công thực tế" 
                           FontSize="13" Foreground="#718096" Margin="0,5,0,0"/>
            </StackPanel>

            <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                <Border Background="White" CornerRadius="8" Margin="0,0,15,0" Padding="15,5">
                    <Border.Effect>
                        <DropShadowEffect BlurRadius="10" Opacity="0.05"/>
                    </Border.Effect>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="Kỳ lương:" VerticalAlignment="Center" Foreground="#718096" Margin="0,0,10,0"/>

                        <TextBlock Text="Tháng " VerticalAlignment="Center" FontWeight="Bold"/>
                        <ComboBox ItemsSource="{Binding Months}" SelectedItem="{Binding SelectedMonth}" 
                                  Width="50" FontWeight="Bold" Margin="5,0"/>

                        <TextBlock Text="/" VerticalAlignment="Center" Margin="5,0"/>

                        <ComboBox ItemsSource="{Binding Years}" SelectedItem="{Binding SelectedYear}" 
                                  Width="70" FontWeight="Bold"/>
                    </StackPanel>
                </Border>

                <Button Command="{Binding CalculatePayrollCommand}"
                        Background="#1A3D64" Foreground="White" Height="45" Padding="20,0" BorderThickness="0" Cursor="Hand">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="8"/>
                        </Style>
                    </Button.Resources>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="⚡" Margin="0,0,10,0"/>
                        <TextBlock Text="TÍNH TOÁN" FontWeight="Bold"/>
                    </StackPanel>
                </Button>
            </StackPanel>
        </Grid>

        <Grid Grid.Row="1" Margin="0,0,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Background="White" CornerRadius="10" Margin="0,0,15,0" Padding="20">
                <Border.Effect>
                    <DropShadowEffect BlurRadius="15" Opacity="0.1" ShadowDepth="2" Color="#ccc"/>
                </Border.Effect>
                <StackPanel>
                    <TextBlock Text="Tổng Thực Lĩnh Dự Kiến" Foreground="#718096" FontSize="13" FontWeight="SemiBold"/>
                    <StackPanel Orientation="Horizontal" Margin="0,10,0,0">
                        <TextBlock Text="{Binding TotalSalaryFund, StringFormat={}{0:N0}}" 
                                   FontSize="28" FontWeight="Bold" Foreground="#2F855A"/>
                        <TextBlock Text=" VNĐ" VerticalAlignment="Bottom" Margin="5,0,0,6" Foreground="#2F855A" FontWeight="Bold"/>
                    </StackPanel>
                </StackPanel>
            </Border>

            <Border Grid.Column="1" Background="White" CornerRadius="10" Margin="0,0,15,0" Padding="20">
                <Border.Effect>
                    <DropShadowEffect BlurRadius="15" Opacity="0.1" ShadowDepth="2" Color="#ccc"/>
                </Border.Effect>
                <StackPanel>
                    <TextBlock Text="Số Nhân Sự Đã Tính" Foreground="#718096" FontSize="13" FontWeight="SemiBold"/>
                    <TextBlock Text="{Binding PayrollList.Count, StringFormat={}{0} người}" 
                               FontSize="28" FontWeight="Bold" Foreground="#1A3D64" Margin="0,10,0,0"/>
                </StackPanel>
            </Border>

            <Border Grid.Column="2" Background="#E6FFFA" CornerRadius="10" Padding="20">
                <StackPanel VerticalAlignment="Center">
                    <TextBlock Text="💡 Logic hệ thống:" FontWeight="Bold" Foreground="#2C7A7B"/>
                    <TextBlock Text="Hệ thống tự động quét Hợp đồng và Chấm công để tính toán." 
                               TextWrapping="Wrap" Margin="0,5,0,0" Foreground="#285E61" FontStyle="Italic"/>
                </StackPanel>
            </Border>
        </Grid>

        <Border Grid.Row="2" Background="White" CornerRadius="10" Padding="10">
            <Border.Effect>
                <DropShadowEffect BlurRadius="15" Opacity="0.1" ShadowDepth="2" Color="#ccc"/>
            </Border.Effect>

            <DataGrid ItemsSource="{Binding PayrollList}" 
          AutoGenerateColumns="False" 
          CanUserAddRows="False" 
          IsReadOnly="True" 
          GridLinesVisibility="Horizontal"
          HorizontalGridLinesBrush="#F0F0F0"
          VerticalContentAlignment="Center"
          Background="White" 
          BorderThickness="0" 
          HeadersVisibility="Column"
          RowHeight="55">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="ID" Binding="{Binding EmployeeId}" Width="60" Foreground="#718096">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="HorizontalAlignment" Value="Center"/>
                                <Setter Property="VerticalAlignment" Value="Center"/>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>

                    <DataGridTemplateColumn Header="HỌ VÀ TÊN" Width="2*">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding FullName}" 
                               FontWeight="SemiBold" 
                               Foreground="#1A3D64" 
                               VerticalAlignment="Center" 
                               Margin="5,0,0,0"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <DataGridTextColumn Header="PHÒNG BAN" Binding="{Binding DepartmentName}" Width="1.5*" Foreground="#718096">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="VerticalAlignment" Value="Center"/>
                                <Setter Property="Padding" Value="5,0,0,0"/>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>

                    <DataGridTextColumn Header="LƯƠNG HĐ" 
                            Binding="{Binding ContractSalary, StringFormat={}{0:N0}}" 
                            Width="140" 
                            FontWeight="SemiBold" 
                            Foreground="#4A5568">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="HorizontalAlignment" Value="Right"/>
                                <Setter Property="VerticalAlignment" Value="Center"/>
                                <Setter Property="Padding" Value="0,0,20,0"/>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                        <DataGridTextColumn.HeaderStyle>
                            <Style TargetType="DataGridColumnHeader">
                                <Setter Property="HorizontalContentAlignment" Value="Right"/>
                                <Setter Property="Padding" Value="0,0,20,10"/>
                                <Setter Property="Background" Value="Transparent"/>
                                <Setter Property="Foreground" Value="#718096"/>
                                <Setter Property="FontWeight" Value="SemiBold"/>
                            </Style>
                        </DataGridTextColumn.HeaderStyle>
                    </DataGridTextColumn>

                    <DataGridTemplateColumn Header="NGÀY CÔNG" Width="120">
                        <DataGridTemplateColumn.HeaderStyle>
                            <Style TargetType="DataGridColumnHeader">
                                <Setter Property="HorizontalContentAlignment" Value="Center"/>
                                <Setter Property="Padding" Value="0,0,0,10"/>
                                <Setter Property="Background" Value="Transparent"/>
                                <Setter Property="Foreground" Value="#718096"/>
                                <Setter Property="FontWeight" Value="SemiBold"/>
                            </Style>
                        </DataGridTemplateColumn.HeaderStyle>

                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Border Background="#EDF2F7" 
                            CornerRadius="5" 
                            Padding="15,5" 
                            HorizontalAlignment="Center" 
                            VerticalAlignment="Center">
                                    <TextBlock Text="{Binding ActualWorkDays}" 
                                   FontWeight="Bold" 
                                   Foreground="#2D3748"
                                   HorizontalAlignment="Center"/>
                                </Border>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <DataGridTemplateColumn Header="THỰC LĨNH" Width="1.5*">
                        <DataGridTemplateColumn.HeaderStyle>
                            <Style TargetType="DataGridColumnHeader">
                                <Setter Property="HorizontalContentAlignment" Value="Right"/>
                                <Setter Property="Padding" Value="0,0,20,10"/>
                                <Setter Property="Background" Value="Transparent"/>
                                <Setter Property="Foreground" Value="#718096"/>
                                <Setter Property="FontWeight" Value="SemiBold"/>
                            </Style>
                        </DataGridTemplateColumn.HeaderStyle>

                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding NetSalary, StringFormat={}{0:N0} đ}" 
                               FontWeight="Bold" 
                               Foreground="#2F855A" 
                               FontSize="14" 
                               VerticalAlignment="Center"
                               HorizontalAlignment="Right"
                               Margin="0,0,20,0"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</UserControl>