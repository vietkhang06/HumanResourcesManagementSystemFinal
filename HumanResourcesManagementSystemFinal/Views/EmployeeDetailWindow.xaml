<Window x:Class="HumanResourcesManagementSystemFinal.Views.EmployeeDetailWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:HumanResourcesManagementSystemFinal.ViewModels"
        mc:Ignorable="d"
        d:DataContext="{d:DesignInstance Type=vm:EmployeeDetailViewModel}"
        Title="Hồ Sơ Nhân Viên" Height="650" Width="900"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        MouseLeftButtonDown="Window_MouseLeftButtonDown">

    <Border Background="White" CornerRadius="20" Margin="20">
        <Border.Effect>
            <DropShadowEffect BlurRadius="30" ShadowDepth="10" Opacity="0.15" Color="#1E293B"/>
        </Border.Effect>

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="280"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" CornerRadius="20,0,0,20">
                <Border.Background>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                        <GradientStop Color="#1A3D64" Offset="0"/>
                        <GradientStop Color="#1A3D64 " Offset="1"/>
                    </LinearGradientBrush>
                </Border.Background>

                <StackPanel Margin="25,50,25,25">
                    <Border Width="130" Height="130" CornerRadius="65" Background="White" Margin="0,0,0,20" BorderThickness="3" BorderBrush="White">
                        <Border.Effect>
                            <DropShadowEffect BlurRadius="10" Opacity="0.3"/>
                        </Border.Effect>
                        <Grid>
                            <Ellipse>
                                <Ellipse.Fill>
                                    <ImageBrush ImageSource="{Binding Employee.EmployeeID, Converter={StaticResource IdToImageConverter}}" Stretch="UniformToFill"/>
                                </Ellipse.Fill>
                            </Ellipse>
                        </Grid>
                    </Border>

                    <TextBlock Text="{Binding Employee.FullName}" 
                               Foreground="White" FontSize="22" FontWeight="Bold" 
                               TextWrapping="Wrap" TextAlignment="Center" Margin="0,0,0,8"/>

                    <Border Background="#30FFFFFF" CornerRadius="12" Padding="15,5" HorizontalAlignment="Center" Margin="0,0,0,30">
                        <TextBlock Text="{Binding Employee.Position.PositionName}" 
                                   Foreground="#E2E8F0" FontSize="13" FontWeight="SemiBold"/>
                    </Border>

                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,10">
                        <TextBlock Text="Mã NV: " Foreground="#94A3B8"/>
                        <TextBlock Text="{Binding Employee.EmployeeID}" Foreground="White" FontWeight="Bold"/>
                    </StackPanel>

                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                        <TextBlock Text="Trạng thái: " Foreground="#94A3B8"/>
                        <Border Background="#10B981" CornerRadius="4" Padding="6,1" Margin="5,0,0,0">
                            <TextBlock Text="{Binding Employee.Status}" Foreground="White" FontSize="11" FontWeight="Bold"/>
                        </Border>
                    </StackPanel>
                </StackPanel>
            </Border>

            <Grid Grid.Column="1">
                <Grid.RowDefinitions>
                    <RowDefinition Height="60"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="80"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Text="HỒ SƠ CÁ NHÂN" VerticalAlignment="Center" Margin="40,0,0,0"
                           Foreground="#1E293B" FontWeight="Bold" FontSize="20"/>

                <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                    <StackPanel Margin="40,10,40,20">

                        <TextBlock Text="Thông tin chung" Foreground="#64748B" FontWeight="Bold" FontSize="13" Margin="0,0,0,15"/>
                        <Border Background="#F8FAFC" CornerRadius="12" Padding="20" Margin="0,0,0,25">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>

                                <StackPanel Grid.Row="0" Grid.Column="0" Margin="0,0,0,15">
                                    <TextBlock Text="Ngày sinh" Foreground="#94A3B8" FontSize="12"/>
                                    <TextBlock Text="{Binding Employee.DateOfBirth, StringFormat='dd/MM/yyyy'}" Foreground="#334155" FontWeight="SemiBold" FontSize="14" Margin="0,3,0,0"/>
                                </StackPanel>

                                <StackPanel Grid.Row="1" Grid.Column="0" Margin="0,0,0,15">
                                    <TextBlock Text="Giới tính" Foreground="#94A3B8" FontSize="12"/>
                                    <TextBlock Text="{Binding Employee.Gender}" Foreground="#334155" FontWeight="SemiBold" FontSize="14" Margin="0,3,0,0"/>
                                </StackPanel>

                                <StackPanel Grid.Row="0" Grid.Column="1" Margin="0,0,0,15">
                                    <TextBlock Text="CCCD/CMND" Foreground="#94A3B8" FontSize="12"/>
                                    <TextBlock Text="{Binding Employee.CCCD}" Foreground="#334155" FontWeight="SemiBold" FontSize="14" Margin="0,3,0,0"/>
                                </StackPanel>

                                <StackPanel Grid.Row="2" Grid.ColumnSpan="2">
                                    <TextBlock Text="Địa chỉ thường trú" Foreground="#94A3B8" FontSize="12"/>
                                    <TextBlock Text="{Binding Employee.Address}" Foreground="#334155" FontWeight="SemiBold" FontSize="14" TextWrapping="Wrap" Margin="0,3,0,0"/>
                                </StackPanel>
                            </Grid>
                        </Border>

                        <TextBlock Text="Liên hệ &amp; Công việc" Foreground="#64748B" FontWeight="Bold" FontSize="13" Margin="0,0,0,15"/>
                        <Border Background="#F8FAFC" CornerRadius="12" Padding="20">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>

                                <StackPanel Grid.Row="0" Grid.Column="0" Margin="0,0,0,15">
                                    <TextBlock Text="Email" Foreground="#94A3B8" FontSize="12"/>
                                    <TextBlock Text="{Binding Employee.Email}" Foreground="#334155" FontWeight="SemiBold" FontSize="14" Margin="0,3,0,0"/>
                                </StackPanel>

                                <StackPanel Grid.Row="0" Grid.Column="1" Margin="0,0,0,15">
                                    <TextBlock Text="Số điện thoại" Foreground="#94A3B8" FontSize="12"/>
                                    <TextBlock Text="{Binding Employee.PhoneNumber}" Foreground="#334155" FontWeight="SemiBold" FontSize="14" Margin="0,3,0,0"/>
                                </StackPanel>

                                <StackPanel Grid.Row="1" Grid.Column="0" Margin="0,0,0,15">
                                    <TextBlock Text="Phòng ban" Foreground="#94A3B8" FontSize="12"/>
                                    <TextBlock Text="{Binding Employee.Department.DepartmentName}" Foreground="#0EA5E9" FontWeight="Bold" FontSize="14" Margin="0,3,0,0"/>
                                </StackPanel>

                                <StackPanel Grid.Row="1" Grid.Column="1" Margin="0,0,0,15">
                                    <TextBlock Text="Quản lý trực tiếp" Foreground="#94A3B8" FontSize="12"/>
                                    <TextBlock Text="{Binding Employee.ManagerID, TargetNullValue='(Không có)'}" Foreground="#334155" FontWeight="SemiBold" FontSize="14" Margin="0,3,0,0"/>
                                </StackPanel>
                            </Grid>
                        </Border>
                    </StackPanel>
                </ScrollViewer>

                <Grid Grid.Row="2" Margin="40,0,40,0">

                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Left" VerticalAlignment="Center">
                        <Button Command="{Binding ExportWordCommand}" Margin="0,0,15,0"
                                Height="45" Padding="20,0" Background="#FF043AD6" Cursor="Hand" BorderThickness="0">
                            <Button.Content>
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="Xuất Hồ Sơ (Word)" Foreground="White" FontWeight="SemiBold"/>
                                </StackPanel>
                            </Button.Content>
                            <Button.Resources>
                                <Style TargetType="Border">
                                    <Setter Property="CornerRadius" Value="10"/>
                                </Style>
                            </Button.Resources>
                        </Button>

                        <Button Command="{Binding ExportPdfCommand}" 
                                CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=Window}}"
                                Height="45" Padding="20,0" Background="#FF711E59" Cursor="Hand" BorderThickness="0">
                            <Button.Content>
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="In / PDF" Foreground="White" FontWeight="SemiBold"/>
                                </StackPanel>
                            </Button.Content>
                            <Button.Resources>
                                <Style TargetType="Border">
                                    <Setter Property="CornerRadius" Value="10"/>
                                </Style>
                            </Button.Resources>
                        </Button>
                    </StackPanel>

                    <Button Content="Đóng" 
                            HorizontalAlignment="Right" VerticalAlignment="Center"
                            Height="45" Width="100"
                            Background="#F1F5F9" Foreground="#64748B" FontSize="14" FontWeight="Bold" 
                            BorderThickness="0" Cursor="Hand"
                            Click="BtnClose_Click">
                        <Button.Resources>
                            <Style TargetType="Border">
                                <Setter Property="CornerRadius" Value="10"/>
                            </Style>
                        </Button.Resources>
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="Button">
                                            <Border x:Name="bdr" Background="{TemplateBinding Background}" CornerRadius="10">
                                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            </Border>
                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter TargetName="bdr" Property="Background" Value="#E2E8F0"/>
                                                    <Setter Property="Foreground" Value="#334155"/>
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </Button.Style>
                    </Button>

                </Grid>
            </Grid>
        </Grid>
    </Border>
</Window>