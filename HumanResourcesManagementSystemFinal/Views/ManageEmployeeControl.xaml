<UserControl x:Class="HumanResourcesManagementSystemFinal.Views.ManageEmployeeControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:vm="clr-namespace:HumanResourcesManagementSystemFinal.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="1000" Background="Transparent">

    <d:UserControl.DataContext>
        <vm:ManageEmployeeViewModel/>
    </d:UserControl.DataContext>
    <UserControl.DataContext>
        <vm:ManageEmployeeViewModel/>
    </UserControl.DataContext>

    <UserControl.Resources>
        <Style TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#718096"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="10,0,10,10"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="BorderThickness" Value="0,0,0,1"/>
            <Setter Property="BorderBrush" Value="#E2E8F0"/>
        </Style>
        <Style TargetType="DataGridCell">
            <Setter Property="Padding" Value="10"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="DataGridCell">
                        <Border Padding="{TemplateBinding Padding}" Background="Transparent">
                            <ContentPresenter VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Foreground" Value="Black"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        <Style TargetType="DataGridRow">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderThickness" Value="0,0,0,1"/>
            <Setter Property="BorderBrush" Value="#F7FAFC"/>
            <Setter Property="Height" Value="50"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#F7FAFC"/>
                </Trigger>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="#EBF8FF"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Background="White" CornerRadius="8" Width="300" Height="45">
                <Border.Effect>
                    <DropShadowEffect BlurRadius="10" Opacity="0.1" ShadowDepth="2" Color="#ccc"/>
                </Border.Effect>
                <Grid>
                    <TextBlock Text="🔍" VerticalAlignment="Center" Margin="15,0,0,0" Foreground="#A0AEC0"/>
                    <TextBox Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                             VerticalContentAlignment="Center" BorderThickness="0" Background="Transparent" 
                             Margin="40,0,10,0" FontSize="14"/>
                </Grid>
            </Border>

            <ComboBox Grid.Column="1" Margin="15,0,0,0" Width="180" Height="45" 
                      ItemsSource="{Binding Departments}" DisplayMemberPath="DepartmentName"
                      SelectedIndex="0" Background="White" BorderThickness="0" Padding="10,0">
                <ComboBox.Resources>
                    <Style TargetType="Border">
                        <Setter Property="CornerRadius" Value="8"/>
                    </Style>
                </ComboBox.Resources>
            </ComboBox>

            <Button Grid.Column="3" Command="{Binding AddEmployeeCommand}"
                    Background="#1A3D64" Foreground="White" Height="45" Padding="20,0" BorderThickness="0" Cursor="Hand">
                <Button.Resources>
                    <Style TargetType="Border">
                        <Setter Property="CornerRadius" Value="8"/>
                    </Style>
                </Button.Resources>
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="➕" Margin="0,0,10,0"/>
                    <TextBlock Text="Thêm Nhân Viên" FontWeight="Bold"/>
                </StackPanel>
            </Button>
        </Grid>

        <Border Grid.Row="1" Background="White" CornerRadius="10" Padding="10">
            <Border.Effect>
                <DropShadowEffect BlurRadius="15" Opacity="0.1" ShadowDepth="2" Color="#ccc"/>
            </Border.Effect>

            <DataGrid ItemsSource="{Binding Employees}" AutoGenerateColumns="False" 
                      CanUserAddRows="False" IsReadOnly="True" GridLinesVisibility="None" 
                      Background="White" BorderThickness="0" HeadersVisibility="Column">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="MÃ NV" Binding="{Binding EmployeeID}" Width="80" Foreground="#718096"/>

                    <DataGridTemplateColumn Header="HỌ VÀ TÊN" Width="2.5*">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <Border Width="35" Height="35" CornerRadius="17.5" Background="#E2E8F0" Margin="0,0,10,0">
                                        <TextBlock Text="{Binding FullName[0]}" HorizontalAlignment="Center" VerticalAlignment="Center" FontWeight="Bold" Foreground="#4A5568"/>
                                    </Border>
                                    <StackPanel VerticalAlignment="Center">
                                        <TextBlock Text="{Binding FullName}" FontWeight="SemiBold" Foreground="#2D3748"/>
                                        <TextBlock Text="{Binding Email}" FontSize="11" Foreground="#718096"/>
                                    </StackPanel>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <DataGridTextColumn Header="PHÒNG BAN" Binding="{Binding Department.DepartmentName}" Width="1.5*" Foreground="#4A5568"/>
                    <DataGridTextColumn Header="CHỨC VỤ" Binding="{Binding Position.Title}" Width="1.5*" Foreground="#4A5568"/>
                    <DataGridTextColumn Header="SĐT" Binding="{Binding PhoneNumber}" Width="150" Foreground="#718096"/>
                    <DataGridTextColumn Header="ĐỊA CHỈ" Binding="{Binding Address}" Width="200" Foreground="#4A5568"/>

                    <DataGridTemplateColumn Header="HÀNH ĐỘNG" Width="100">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <Button Content="✏️" Command="{Binding DataContext.EditEmployeeCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" 
                                            CommandParameter="{Binding}" Background="Transparent" BorderThickness="0" Cursor="Hand" Margin="0,0,10,0"/>
                                    <Button Content="🗑️" Command="{Binding DataContext.DeleteEmployeeCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" 
                                            CommandParameter="{Binding}" Background="Transparent" BorderThickness="0" Cursor="Hand" Foreground="#E53E3E"/>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Border>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15,0,0">
            <TextBlock Text="Hiển thị 1-10" VerticalAlignment="Center" Foreground="#718096" Margin="0,0,20,0"/>
            <Button Content="&lt;" Width="30" Height="30" Background="White" BorderBrush="#E2E8F0" BorderThickness="1"/>
            <Button Content="1" Width="30" Height="30" Background="#1A3D64" Foreground="White" BorderThickness="0" Margin="5,0"/>
            <Button Content=">" Width="30" Height="30" Background="White" BorderBrush="#E2E8F0" BorderThickness="1"/>
        </StackPanel>
    </Grid>
</UserControl>