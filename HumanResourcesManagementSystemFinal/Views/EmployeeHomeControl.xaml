<UserControl x:Class="HumanResourcesManagementSystemFinal.Views.EmployeeHomeControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:vm="clr-namespace:HumanResourcesManagementSystemFinal.ViewModels"
             xmlns:converters="clr-namespace:HumanResourcesManagementSystemFinal.Converters"
             mc:Ignorable="d" 
             d:DesignHeight="850" d:DesignWidth="1200" Background="Transparent">

    <UserControl.DataContext>
        <vm:EmployeeHomeViewModel/>
    </UserControl.DataContext>

    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>
        <converters:BinaryImageConverter x:Key="BinaryImageConverter"/>

        <Style x:Key="HeaderStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="#1E293B"/>
            <Setter Property="Margin" Value="0,0,0,15"/>
        </Style>

        <Style x:Key="CardPanel" TargetType="Border">
            <Setter Property="Background" Value="White"/>
            <Setter Property="CornerRadius" Value="12"/>
            <Setter Property="Padding" Value="20"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect BlurRadius="15" Opacity="0.05" ShadowDepth="4" Color="Black"/>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="White"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="#94A3B8"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="Padding" Value="10,10"/>
            <Setter Property="BorderThickness" Value="0,0,0,1"/>
            <Setter Property="BorderBrush" Value="#E2E8F0"/>
        </Style>
        <Style TargetType="DataGridRow">
            <Setter Property="Background" Value="White"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Padding" Value="0,12"/>
            <Setter Property="BorderThickness" Value="0,0,0,1"/>
            <Setter Property="BorderBrush" Value="#F8FAFC"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#F8FAFC"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,10,0,25">
            <TextBlock Text="Chào mừng quay trở lại," FontSize="16" Foreground="#64748B"/>
            <TextBlock Text="{Binding WelcomeMessage, FallbackValue='Nhân Viên'}" 
                       FontSize="28" FontWeight="ExtraBold" Foreground="#1E3A8A"/>
        </StackPanel>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="720"/>
                <ColumnDefinition Width="20"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Grid Grid.Column="0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="20"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <Grid Grid.Row="0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="15"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="15"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <Border Grid.Column="0" Style="{StaticResource CardPanel}">
                        <StackPanel VerticalAlignment="Center">
                            <TextBlock Text="Vào Ca Lúc" FontSize="12" Foreground="#64748B" FontWeight="SemiBold" Margin="0,0,0,5"/>
                            <TextBlock Text="{Binding TodayCheckInTime, FallbackValue='--:--'}" FontSize="28" FontWeight="Bold" Foreground="#2C7A7B"/>
                        </StackPanel>
                    </Border>

                    <Border Grid.Column="2" Style="{StaticResource CardPanel}">
                        <StackPanel VerticalAlignment="Center">
                            <TextBlock Text="Phép Còn Lại" FontSize="12" Foreground="#64748B" FontWeight="SemiBold" Margin="0,0,0,5"/>
                            <TextBlock Text="{Binding RemainingLeaveDays, FallbackValue=12}" FontSize="28" FontWeight="Bold" Foreground="#D69E2E"/>
                        </StackPanel>
                    </Border>

                    <Border Grid.Column="4" Style="{StaticResource CardPanel}">
                        <StackPanel VerticalAlignment="Center">
                            <TextBlock Text="Công Tháng Này" FontSize="12" Foreground="#64748B" FontWeight="SemiBold" Margin="0,0,0,5"/>
                            <TextBlock Text="{Binding DaysWorkedThisMonth, FallbackValue=0 ngày, StringFormat=\{0\} ngày}" FontSize="28" FontWeight="Bold" Foreground="#2B6CB0"/>
                        </StackPanel>
                    </Border>
                </Grid>

                <Border Grid.Row="2" Style="{StaticResource CardPanel}" VerticalAlignment="Stretch">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <DockPanel Grid.Row="0" LastChildFill="False" Margin="0,0,0,15">
                            <TextBlock Text="Trạng Thái Đơn Của Tôi" Style="{StaticResource HeaderStyle}" Margin="0"/>
                            <Border Background="#F1F5F9" CornerRadius="12" Padding="12,4" DockPanel.Dock="Right">
                                <TextBlock Text="{Binding MyLeaveRequests.Count, StringFormat='{}{0} Đơn'}" FontSize="12" FontWeight="SemiBold" Foreground="#64748B"/>
                            </Border>
                        </DockPanel>

                        <DataGrid Grid.Row="1" ItemsSource="{Binding MyLeaveRequests}" 
                                  AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True" 
                                  GridLinesVisibility="Horizontal" HeadersVisibility="Column"
                                  Background="White" BorderThickness="0"
                                  HorizontalGridLinesBrush="#F1F5F9" RowHeaderWidth="0"
                                  RowHeight="50"
                                  VerticalScrollBarVisibility="Auto">
                            <DataGrid.Columns>
                                <DataGridTemplateColumn Header="LÝ DO" Width="2*">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <TextBlock Text="{Binding Reason}" FontWeight="SemiBold" Foreground="#334155" 
                                                       TextTrimming="CharacterEllipsis" VerticalAlignment="Center" Margin="10,0"/>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>
                                <DataGridTemplateColumn Header="THỜI GIAN" Width="*">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <TextBlock Foreground="#64748B" FontSize="12" VerticalAlignment="Center">
                                                <Run Text="{Binding StartDate, StringFormat=dd/MM}"/>
                                                <Run Text=" ➜ "/>
                                                <Run Text="{Binding EndDate, StringFormat=dd/MM}"/>
                                            </TextBlock>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>
                                <DataGridTemplateColumn Header="TRẠNG THÁI" Width="Auto">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <Border CornerRadius="6" Padding="8,4" HorizontalAlignment="Left" VerticalAlignment="Center">
                                                <TextBlock Text="{Binding Status}" FontSize="11" FontWeight="Bold"/>
                                                <Border.Style>
                                                    <Style TargetType="Border">
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding Status}" Value="Pending">
                                                                <Setter Property="Background" Value="#FEFCBF"/>
                                                                <Setter Property="TextBlock.Foreground" Value="#744210"/>
                                                            </DataTrigger>
                                                            <DataTrigger Binding="{Binding Status}" Value="Đang chờ">
                                                                <Setter Property="Background" Value="#FEFCBF"/>
                                                                <Setter Property="TextBlock.Foreground" Value="#744210"/>
                                                            </DataTrigger>
                                                            <DataTrigger Binding="{Binding Status}" Value="Approved">
                                                                <Setter Property="Background" Value="#C6F6D5"/>
                                                                <Setter Property="TextBlock.Foreground" Value="#22543D"/>
                                                            </DataTrigger>
                                                            <DataTrigger Binding="{Binding Status}" Value="Đã duyệt">
                                                                <Setter Property="Background" Value="#C6F6D5"/>
                                                                <Setter Property="TextBlock.Foreground" Value="#22543D"/>
                                                            </DataTrigger>
                                                            <DataTrigger Binding="{Binding Status}" Value="Rejected">
                                                                <Setter Property="Background" Value="#FED7D7"/>
                                                                <Setter Property="TextBlock.Foreground" Value="#822727"/>
                                                            </DataTrigger>
                                                            <DataTrigger Binding="{Binding Status}" Value="Từ chối">
                                                                <Setter Property="Background" Value="#FED7D7"/>
                                                                <Setter Property="TextBlock.Foreground" Value="#822727"/>
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </Border.Style>
                                            </Border>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>
                            </DataGrid.Columns>
                        </DataGrid>
                    </Grid>
                </Border>
            </Grid>

            <Grid Grid.Column="2">
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="20"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <Border Grid.Row="0" Style="{StaticResource CardPanel}" VerticalAlignment="Stretch">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <DockPanel Grid.Row="0" LastChildFill="False" Margin="0,0,0,15">
                            <TextBlock Text="Thông Báo Mới" Style="{StaticResource HeaderStyle}" Margin="0"/>
                            <Border Background="#EFF6FF" CornerRadius="12" Padding="12,4" DockPanel.Dock="Right">
                                <TextBlock Text="New" FontSize="11" FontWeight="Bold" Foreground="#2563EB"/>
                            </Border>
                        </DockPanel>

                        <DataGrid Grid.Row="1" ItemsSource="{Binding Notifications}" 
                                  AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True"
                                  GridLinesVisibility="Horizontal" HeadersVisibility="Column"
                                  Background="White" BorderThickness="0"
                                  HorizontalGridLinesBrush="#F1F5F9" RowHeaderWidth="0"
                                  VerticalScrollBarVisibility="Auto">
                            <DataGrid.Columns>
                                <DataGridTemplateColumn Header="NỘI DUNG" Width="*">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <StackPanel Margin="5,0">
                                                <TextBlock Text="{Binding Title}" FontWeight="Bold" Foreground="#334155" TextWrapping="Wrap"/>
                                                <TextBlock Text="{Binding Content}" FontSize="11" Foreground="#64748B" TextTrimming="CharacterEllipsis"/>
                                            </StackPanel>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>
                                <DataGridTemplateColumn Header="NGÀY" Width="Auto">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <TextBlock Text="{Binding Date, StringFormat='dd/MM'}" 
                                                       VerticalAlignment="Center" FontSize="11" Foreground="#94A3B8" Margin="10,0"/>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>
                            </DataGrid.Columns>
                        </DataGrid>
                    </Grid>
                </Border>

                <Border Grid.Row="2" Style="{StaticResource CardPanel}">
                    <StackPanel>
                        <TextBlock Text="Lối Tắt" Style="{StaticResource HeaderStyle}"/>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="10"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <Button Grid.Column="0" Command="{Binding OpenLeavePopupCommand}" Cursor="Hand" BorderThickness="0">
                                <Button.Template>
                                    <ControlTemplate TargetType="{x:Type Button}">
                                        <Border Background="White" BorderBrush="#E2E8F0" BorderThickness="1" CornerRadius="12" Padding="10">
                                            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                                                <TextBlock Text="📝" FontSize="20" HorizontalAlignment="Center" Margin="0,0,0,5"/>
                                                <TextBlock Text="Tạo Đơn Xin Nghỉ" FontWeight="Bold" Foreground="#2D3748" FontSize="13" HorizontalAlignment="Center"/>
                                            </StackPanel>
                                            <Border.Style>
                                                <Style TargetType="Border">
                                                    <Style.Triggers>
                                                        <Trigger Property="IsMouseOver" Value="True">
                                                            <Setter Property="Background" Value="#F8FAFC"/>
                                                            <Setter Property="BorderBrush" Value="#2563EB"/>
                                                        </Trigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </Border.Style>
                                        </Border>
                                    </ControlTemplate>
                                </Button.Template>
                            </Button>

                            <Button Grid.Column="2" 
                                    Command="{Binding QuickCheckInCommand}" 
                                    IsEnabled="{Binding IsCheckInEnabled}"
                                    Cursor="Hand" BorderThickness="0">
                                <Button.Template>
                                    <ControlTemplate TargetType="{x:Type Button}">
                                        <Border Background="{Binding DataContext.CheckInButtonColor, RelativeSource={RelativeSource AncestorType=UserControl}}" 
                                                CornerRadius="12" Padding="10">
                                            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                                                <TextBlock Text="{Binding DataContext.CheckInButtonIcon, RelativeSource={RelativeSource AncestorType=UserControl}}" 
                                                           FontSize="20" HorizontalAlignment="Center" Margin="0,0,0,5" Foreground="White"/>
                                                <TextBlock Text="{Binding DataContext.CheckInButtonContent, RelativeSource={RelativeSource AncestorType=UserControl}}" 
                                                           FontWeight="Bold" Foreground="White" FontSize="13" HorizontalAlignment="Center"/>
                                            </StackPanel>
                                            <Border.Style>
                                                <Style TargetType="Border">
                                                    <Style.Triggers>
                                                        <Trigger Property="IsMouseOver" Value="True">
                                                            <Setter Property="Opacity" Value="0.9"/>
                                                        </Trigger>
                                                        <DataTrigger Binding="{Binding IsEnabled, RelativeSource={RelativeSource AncestorType=Button}}" Value="False">
                                                            <Setter Property="Opacity" Value="0.5"/>
                                                            <Setter Property="Cursor" Value="No"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </Border.Style>
                                        </Border>
                                    </ControlTemplate>
                                </Button.Template>
                            </Button>
                        </Grid>
                    </StackPanel>
                </Border>
            </Grid>
        </Grid>
    </Grid>
</UserControl>