<Window x:Class="HumanResourcesManagementSystemFinal.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:views="clr-namespace:HumanResourcesManagementSystemFinal.Views" 
        mc:Ignorable="d"
        Title="Hệ thống Đăng nhập" Height="550" Width="900"
        WindowStartupLocation="CenterScreen" 
        WindowStyle="None" ResizeMode="NoResize"
        AllowsTransparency="True" Background="Transparent">

    <Border Background="#f9f8f6" CornerRadius="25">
        <Border.Effect>
            <DropShadowEffect BlurRadius="30" ShadowDepth="5" Opacity="0" Color="#202020"/>
        </Border.Effect>

        <Border.OpacityMask>
            <VisualBrush>
                <VisualBrush.Visual>
                    <Border Background="Black" CornerRadius="25"
                            Width="{Binding ActualWidth, RelativeSource={RelativeSource AncestorType=Border}}"
                            Height="{Binding ActualHeight, RelativeSource={RelativeSource AncestorType=Border}}"/>
                </VisualBrush.Visual>
            </VisualBrush>
        </Border.OpacityMask>

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1.2*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Grid Grid.Column="0">
                <Image Source="/Images/png1.png" HorizontalAlignment="Right" Stretch="UniformToFill">
                    <Image.Effect>
                        <BlurEffect Radius="15" KernelType="Gaussian"/>
                    </Image.Effect>
                </Image>

                <Rectangle>
                    <Rectangle.Fill>
                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                        </LinearGradientBrush>
                    </Rectangle.Fill>
                </Rectangle>

                <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                    <Border Width="80" Height="80" Background="White" CornerRadius="40" Margin="0,0,0,20" Opacity="0.9">
                        <TextBlock Text="HR" FontSize="30" FontWeight="Bold" Foreground="#102f76" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <TextBlock Text="HRMS PRO" FontSize="36" FontWeight="Bold" Foreground="#f9f8f6" HorizontalAlignment="Center">
                        <TextBlock.Effect>
                            <DropShadowEffect ShadowDepth="2" BlurRadius="5" Opacity="0.5"/>
                        </TextBlock.Effect>
                    </TextBlock>
                </StackPanel>
            </Grid>

            <Grid Grid.Column="1">
                <Button Click="CloseButton_Click" Content="✕" HorizontalAlignment="Right" VerticalAlignment="Top"
                        Width="30" Height="30" Margin="0,15,15,0" Panel.ZIndex="100"
                        Background="Transparent" BorderThickness="0" Foreground="#999" FontSize="16" Cursor="Hand"/>

                <ContentControl x:Name="MainFrame" />
            </Grid>
        </Grid>
    </Border>
</Window>